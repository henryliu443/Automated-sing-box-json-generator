# Project Instructions

Before doing smoke tests, server operations, Docker verification, release work,
or any action that may affect the live VPS, read and follow `SMOKE.md`.

The VPS is production infrastructure. Do not run deployment, generator, service
restart, or host-level verification commands on it unless the user has approved
the written plan required by `SMOKE.md`.
